import { NewTransaction } from './new-transaction';

export interface Transaction extends NewTransaction {
  $id: string;
  $createdAt: string;
  $updatedAt: string;
  $permissions: string[];
  $databaseId: string;
  $collectionId: string;
}
